name: Dev CI/CD

on:
  push:
    branches:
      - develop

jobs:
  ci:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: product/frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version-file: product/frontend/.node-version
          cache: npm
          cache-dependency-path: product/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

  deploy-dev:
    needs: ci
    runs-on:
      - self-hosted
      - Linux
      - X64

    steps:
      - name: Deploy DEV
        run: /srv/bugrat/mjtensu-dev/deploy.sh