{
  "schema": "mjtensu.mldb/train-protocol/v1",
  "id": "rotated-fcos-standard-v1",
  "status": "sealed",
  "task": "mahjong-tile-rotated-detection-v1",
  "name": "Standard rotated FCOS training",
  "description": "Self-contained PyTorch training for rotated FCOS-style dense detectors on the immutable MLDB OBB Corpus.",
  "implementation": {
    "entrypoint": "train",
    "sha256": "b8383b50fa4216e0223d8c8ed44fa1c3d9e50a107a39516463cf04c70691f117"
  },
  "parameters": {
    "epochs": {
      "default": 80
    },
    "batch_size": {
      "default": 16
    },
    "learning_rate": {
      "default": 0.0003
    },
    "weight_decay": {
      "default": 0.0001
    },
    "warmup_epochs": {
      "default": 3.0
    },
    "center_radius": {
      "default": 1.5
    },
    "gradient_clip": {
      "default": 10.0
    },
    "amp": {
      "default": true
    },
    "tf32": {
      "default": true
    }
  },
  "notes": {
    "optimizer": "AdamW",
    "scheduler": "warmup-cosine",
    "normalization": "ImageNet RGB mean/std",
    "checkpoint_selection": "final epoch"
  }
}
