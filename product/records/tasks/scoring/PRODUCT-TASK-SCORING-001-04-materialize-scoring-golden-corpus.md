# PRODUCT-TASK-SCORING-001-04: Materialize scoring golden corpus

- **status**: not_started
- **date**: 2026-08-26
- **work_item**: PRODUCT-WORK-SCORING-001
- **task_type**: implementation
- **estimate**: 1.5d
- **depends_on**:
  - PRODUCT-TASK-SYSTEM-001-05
- **outputs**:
  - versioned scoring golden-corpus fixtures
  - PRODUCT-TASK-SCORING-001-04

## Goal

Create the deterministic versioned semantic scoring corpus required to verify the Agari fork and TypeScript adapter against the complete accepted product scoring surface.

## Work

- Define a reviewable fixture schema using product tile/condition/rule/result semantics rather than Agari display strings.
- Add positive ordinary, chiitoitsu, kokushi, yaku, dora/aka, fu, limit, yakuman, and payment cases required by the testing strategy.
- Add rule-switch pairs for kiriage, counted yakuman, double-yakuman variants, multiple yakuman, double-wind pair fu, open tanyao, aka dora, indicator dora, and ippatsu.
- Add negative/not-ready cases distinguishing non-winning shape, no-yaku, and dora-only bonus.
- Record expected yaku IDs/han, dora counts, FuCalculation semantics, LimitClassification, and payment values explicitly.
- Add fixture schema validation/helpers needed for later S06 execution without embedding a second scoring engine.

## Implementation contract

| target | required change | acceptance criterion | verification |
|---|---|---|---|
| corpus fixture schema | Define a deterministic, versioned, product-semantic fixture representation. | Fixtures identify exact input, active rule profile, and expected normalized outcome without depending on Agari display names. | Schema/helper unit tests and reviewable fixture parsing. |
| minimum coverage matrix | Materialize every minimum case class listed in `spec:product.system.contracts.testing_strategy`. | A machine-checkable inventory proves at least one fixture for each required matrix item and both branches of required rule switches. | Corpus coverage-inventory test. |
| expected semantic results | Record explicit expected yaku/han/dora/fu/limit/payment or expected semantic failure state per case. | Critical score semantics are asserted by fields rather than opaque snapshots. | Fixture-schema validation plus later S06 real-engine execution. |
| no second scoring engine | Keep fixtures declarative; do not compute expected outputs from a duplicate TypeScript scoring formula. | Expected values are authored/reviewed test data, not generated by mjtensu scoring logic. | Source review and fixture-helper tests. |

## Done condition

A versioned deterministic golden corpus exists, passes its schema/coverage checks, and covers every minimum scoring case required before real-engine verification.

## Verification

- Run fixture schema validation.
- Run the corpus minimum-coverage inventory check.
- Review representative ordinary, fu, limit, and yakuman fixtures for explicit semantic expected values.
- Confirm no helper calculates expected scoring results from duplicate mahjong formulas.

## Evidence

- `spec:product.system.contracts.testing_strategy` defines the required golden-corpus matrix.
- `spec:product.system.contracts.scoring_api` defines the normalized product result types asserted by the fixtures.
- Execution results are recorded here when the Task is performed.
